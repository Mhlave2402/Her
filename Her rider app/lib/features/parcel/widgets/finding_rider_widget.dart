import 'package:flutter/material.dart';

class FindingRiderWidget extends StatelessWidget {
  const FindingRiderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Finding Rider...'),
    );
  }
}
